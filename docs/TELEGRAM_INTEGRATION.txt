==========================================================
SENTINEL-Q: TELEGRAM INTEGRATION GUIDE
==========================================================

Requirements:
- BotFather token
- UserInfoBot chat ID
- matching values in `.env`

Security:
- never commit `.env`
- whoever has the bot token can control the bot

Implementation notes:
- `parse_mode="Markdown"` improves readability with formatting and emoji
- `httpx.AsyncClient()` keeps message sending non-blocking
- if the token is missing, the system logs the issue and keeps running
==========================================================
